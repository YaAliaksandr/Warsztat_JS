const inputsRef = document.querySelectorAll('.calc__input');
const divForDrop = document.querySelector('.select__input');

const liResult = document.querySelectorAll('.list__item ');



console.log(liResult)

// liResult.forEach((li) => this.style.display = "block");
console.log(inputsRef);

console.log(divForDrop);


inputsRef.forEach((input) => input.addEventListener('click',))